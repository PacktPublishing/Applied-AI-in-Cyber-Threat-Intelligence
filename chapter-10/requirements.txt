# requirements.txt
# Dependencies for the Chapter 10 Argument Mapping Pipeline (my_agent/agent.py)
# Install with: pip install -r requirements.txt

google-adk          # Google Agent Development Kit (includes google-genai)
python-dotenv       # Loads GOOGLE_API_KEY / GEMINI_API_KEY from .env
pydantic            # Structured output schema (ProductionVerdict)
